# 01 — Guest Mode Navigation Button on Login Screen

**What to build:** An unauthenticated user or guest visiting the login screen sees a prominent "Continue as Guest" outline button directly underneath the "Log in" button. Clicking this button navigates them directly to the main typing screen at `/`.

**Blocked by:** None — can start immediately

**Status:** completed

- [x] A button labeled "Continue as Guest" is rendered in the login card content area below the "Log in" button on the login screen.
- [x] The button is styled as a full-width outline variant button.
- [x] Clicking the "Continue as Guest" button navigates directly to `/`.
- [x] The button remains accessible via keyboard navigation.
- [x] Component contract tests verify button presence, styling variant, and destination target.
